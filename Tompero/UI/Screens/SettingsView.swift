//
//  SettingsView.swift
//  Tompero
//

import SwiftUI

struct SettingsView: View {
    var body: some View {
        ZStack {
            StarsBackground().ignoresSafeArea()
            Text("Settings — TODO")
                .foregroundColor(.white)
        }
    }
}
